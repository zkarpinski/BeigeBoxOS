/**
 * KarpOS virtual filesystem barrel — re-export for `@/app/virtual-fs` (My Computer, etc.).
 * Storage key: `karpos-filesystem-linux`. Independent from Win98 `win98-filesystem`.
 */
export * from './fileSystem';
