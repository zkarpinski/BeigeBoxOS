/**
 * Re-export packaged Minesweeper (logic + UI + Win98 default theme).
 * Theme lives in `@retro-web/app-minesweeper/themes/win98.css` (imported from layout).
 */
export {
  MinesweeperWindow,
  minesweeperAppConfig,
  MINESWEEPER_ICON_SRC,
} from '@retro-web/app-minesweeper';
