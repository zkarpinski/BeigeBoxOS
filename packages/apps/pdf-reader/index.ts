/**
 * @retro-web/app-pdf-reader — mock PDF viewer (iframe + toolbar).
 * Import `themes/base.css` on WinXP; add `themes/karpos.css` on KarpOS.
 */
export { PdfReaderWindow, pdfReaderAppConfig, PDF_READER_ICON_SRC } from './src/PdfReaderWindow';
